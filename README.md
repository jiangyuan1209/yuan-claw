# my-agent

A local CLI coding agent built with TypeScript.

## Requirements

- Node.js 18+
- An API key configured in `.env`

## Setup

Create a `.env` file:

```env
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini